import React from 'react'
import { Map as LeafletMap, TileLayer } from "react-leaflet";


const Map = () => {
    return (
        <div className="map" >
            <LeafletMap>
                
            </LeafletMap>
        </div>
    )
}

export default Map
